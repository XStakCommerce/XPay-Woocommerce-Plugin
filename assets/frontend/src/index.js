import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

/*
import XPay from './App';

const root = ReactDOM.createRoot(document.getElementById('wc_xpay_widget'));

setTimeout(function(){
    root.render(
      <React.StrictMode>
        <XPay />
      </React.StrictMode>
    );
},5000);
*/